/**
 * Placeholder for the generated Supabase schema types.
 *
 * Once a live project exists, replace this file's contents with the output of:
 *   supabase gen types typescript --project-id <project-id> > src/lib/supabase/types.ts
 *
 * `Database` is left loose (not `never`) so callers can compile today; it
 * narrows automatically once the generated types are dropped in.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
